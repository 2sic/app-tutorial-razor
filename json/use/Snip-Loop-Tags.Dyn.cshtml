@inherits Custom.Hybrid.Razor14
@*!
Tut.Tabs=file:./demo.json
!*@
@{
  var someJson = System.IO.File.ReadAllText(App.PhysicalPath + "/json/use/demo.json");
  var thing = AsDynamic(someJson);
}
<ol>
  @foreach (var tag in thing.Tags) {
    <li>@tag</li>
  }
</ol>