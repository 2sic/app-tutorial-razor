@inherits Custom.Hybrid.Razor14
@*!
Tut.Tabs=file:json/dude.json
!*@
@{
  // Variables for the tests
  string jsonRoot = App.PhysicalPath + "/json/quickref/json";

  // Simple JSON strings which could come from a file or web service
  var jDude = System.IO.File.ReadAllText(jsonRoot + "/dude.json");
  var loose = AsDynamic(jDude);
}
<ol>
  <li>
    <code>loose.String("Fake")</code> (typed):
    @loose.Fake
  </li>
  <li>
    <code>loose.String("Fake")</code>:
    @loose.Fake
  </li>
  <li>
    <code>loose.String("Fake") ?? fallback</code>:
    @(loose.Fake ?? "I don't know!")
  </li>
  <li>
    <code>loose.Int("Fake")</code>:
    @loose.Fake
  </li>
  <li>
    <code>loose.Int("Fake") ?? -1</code>:
    @(loose.Fake ?? -1)
  </li>
</ol>
