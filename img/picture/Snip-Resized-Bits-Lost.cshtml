@inherits Custom.Hybrid.Razor14
@*!
Tut.Tabs=Generated HTML
!*@
<hide-silent>@GetCode("/tut-sys/ImgDemoHelpers.cs").ShowCurrentSrc()</hide-silent>
@{
  var imgPath = App.Path + "/assets/img-resize";
  var picFullControl = Kit.Image.Picture(imgPath + "/jellyfish-2000.jpg");
}
<picture>
  @foreach (var st in picFullControl.Sources)
  {
    @st
  }
  <img loading="lazy" src='@picFullControl.Src' style='width: 50%' class='border border-primary'>
</picture>
@{/*!*/
  var previewString = "<picture> ";
  foreach (var st in picFullControl.Sources) { previewString += st.ToString(); }
  previewString += "<img loading='lazy' src='" + picFullControl.Src + "' style='width: 50%' class='border border-primary'>";
  previewString += "</picture>";
  DynamicModel.Tutorial.SetTabContents("html-img:" + previewString);
/*!*/}
