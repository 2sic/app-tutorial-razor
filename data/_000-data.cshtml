@inherits Custom.Hybrid.Razor14
<hide>
@Html.Partial("_header.cshtml")
@{  
  var hlp = CreateInstance("../shared/Helpers.cs"); 
}
</hide>

<trim>
<h2>Working with Data</h2>
<div class="row">
  <div class="col-lg-7">
    <p>If you want to work with <strong>data</strong> (like from a database) instead of <strong>@hlp.TutLink("content", "content")</strong> (which are data items assigned to one instance) you need to get it from somewhere and then loop through it. Typical data comes from:</p>
  </div>
  @Html.Partial("../shared/_DefaultInfoSection.cshtml")
</div>
<ul>
  <li>the current App, using <code>App.Data[...]</code></li>
  <li>a query in the App, using <code>App.Query[...]</code></li>
  <li>a SQL database - either through an <code>App.Query</code>, from a <code>DataSource</code> or from a <code>SqlReader</code></li>
  <li>a CSV file - either through an <code>App.Query</code>, from a <code>DataSource</code> or from your own code</li>
  <li>DNN objects like the users - either through an <code>App.Query</code>, from a <code>DataSource</code>, from DNN-objects or from your own code</li>
  <li>a JSON or XML file - usually loaded from your own code</li>
  <li>a WebService - usually loaded from your own code</li>
</ul>
<p>
  Once you have the data, you will usually want to loop-and-show, which is easiest using <code>dynamic</code> objects, so you can just type things like <code>@@Content.FirstName</code> to show the properties. 
</p>
</trim>

@* 
<h2>Data of the current App.Data</h2>

<ol>
  @hlp.TutorialLink("Data from the App using App.Data", "data210")
</ol>

<hr>
<h2>Data from App.Query</h2>

<ol>
  @hlp.TutorialLink("Using App.Query", "data220")
  @hlp.TutorialLink("CSV data from an App.Query", "data230")
  @hlp.TutorialLink("CSV data from an App.Query which is configured in Settings", "data231")
  @hlp.TutorialLink("SQL data from an App.Query", "data310")
  @hlp.TutorialLink("SQL data from an App.Query with Params", "data311")
  @hlp.TutorialLink("App.Query with Params - Run multiple times", "data312")
</ol>

<hr>
<h2>Data from direct SQL</h2>

<ol>
  @hlp.TutorialLink("SQL data from a SQL query and DataTable", "data330")
  @hlp.TutorialLink("SQL data from a SQL query and DataReader", "data331")
</ol>

<hr>
<h2>Data from DNN/DotNetNuke</h2>

<ol>
  @hlp.TutorialLink("DNN data (pages/tabs) from the DNN TabController", "data510")
</ol>

<hr>
<h2>Parent/Child a.k.a. List/Details</h2>

<ol>
  @hlp.TutorialLink("List-Details with one template and code-only", "data610")
  @hlp.TutorialLink("List-Details with three templates and code-only", "data611")
  @hlp.TutorialLink("List-Details with two templates, automatic view-switching", "data620")
  @hlp.TutorialLink("List-Details with two templates, automatic view-switching and query", "data621")
</ol>

<hr>
<h2>Deep Dive into Content-Types (advanced ⚠) @hlp.Highlighted("(new!)")</h2>

<ol>
  @hlp.TutorialLink("Look into a Content-Type", "data910", newText: "(new)")
</ol>


<hr>
<h2>Future Examples (pending)</h2>
<ul class="unready">
  @hlp.TutorialLink("SQL data using PetaPoco", "data340")
</ul> *@
