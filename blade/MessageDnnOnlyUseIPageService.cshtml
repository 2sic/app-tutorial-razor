@inherits Custom.Hybrid.Razor14
@{
  var Sys = GetCode("/Shared/Helpers.cs");
  // Plase this message in a variable, so our quality checks don't trigger a warning
  // The # and if must not be directly connected in the source, as that triggers the warning
  var ifNetCoreApp = "#" + "if " + "NETCOREAPP";
}
<div class="alert alert-warning" role="alert">
  <h3>
    ⚠️ WARNING: These examples <em>only work in Dnn</em> ☢️
  </h3>
  <p>
    Note that to ensure this tutorial can run on all platforms, we've included some <code>@ifNetCoreApp</code> which changes what code runs on what platform.
  </p>
  👉 See also @Sys.TutLink("Razor Page Service", "page/home") 
</div>
