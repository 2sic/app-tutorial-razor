@inherits Custom.Hybrid.Razor12
@using ToSic.Razor.Blade;
<hide>
@Html.Partial("_header.cshtml")
@{  var hlp = CreateInstance("../shared/Helpers.cs"); }
</hide>
<trim>
  <h2>Activate Turn-On and execute basic tasks</h2>
  <div>
    This page activates Turn-On and executes a JavaScript function on initial loading.
    Look at the resulting log in the console to see the effect. 
  </div>
</trim>

@{
  var page = GetService<ToSic.Sxc.Web.IPageService>();
  // activate the Turn-On feature
  page.Activate("turnOn");
}

<script>
  function init() {
    console.log("This function was executed by the 2sxc Turn-On. 😉");
  }

  window.razorTutorial ??= {};
  window.razorTutorial.init = init;
</script>

@* Executes the JavaScript function window.razorTutorial.init() from above *@
<turnOn turn-on='{ "run": "window.razorTutorial.init()" }'></turnOn>

<hide>
@Html.Partial("../shared/_source-code.cshtml", new { Path }) 
</hide>