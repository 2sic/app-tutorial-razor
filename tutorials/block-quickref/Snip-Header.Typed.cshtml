@inherits Custom.Hybrid.RazorTyped
@{/*! Tut.Tabs=ViewConfig */
  var viewCnf = MyModel.Code("Tutorial").ViewConfig;
  var MyItems = AsItems(viewCnf.List(query: "QuickRef-Content-Simulation") as object);
  var MyHeader = AsItem(viewCnf.Header(query: "QuickRef-Content-Simulation", stream: "Header") as object);
/*!*/}
<h3>@MyHeader.Title</h3>
<ul>
  @foreach (var item in MyItems) {
    <li>
      @item.Title
    </li>
  }
</ul>


