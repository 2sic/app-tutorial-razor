@inherits Custom.Hybrid.RazorTyped
@using ToSic.Razor.Blade;
@using System.Linq;
@using ToSic.Eav.DataSources;
@{/*! Tut.Tabs=file:../TreeBasicWithParents.cs*/
/*!*/}
@{
  var tree = Kit.Data.GetSource(name: "TreeBasicWithParents");
  var root = AsDynamic(tree.List.FirstOrDefault(i => i.EntityId == 1));
}
@Html.Partial("../../data-sources/helpers/Show Tree.cshtml",
  new { Title = "Show Tree of Data", Root = root, SubNodeNames = "SubItems", ParentField = "Parent" })

@Html.Partial("../../data-sources/helpers/Show Data List.cshtml", 
  new { Title = "Flat List of Items", Data = tree })